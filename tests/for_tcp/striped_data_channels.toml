[client]
default_token = "default_token_if_not_specify" # security-scan:allow dummy fixture token, same value as the sibling transport fixtures

[client.control]
default_remote_addr = "127.0.0.1:2333"

[client.transport]
type = "plain"

[client.services.echo]
local_addr = "127.0.0.1:8080"
remote_bind_addr = "0.0.0.0:2334"

[server]
default_token = "default_token_if_not_specify" # security-scan:allow dummy fixture token, same value as the sibling transport fixtures
allow_ports = ["2334-2337"]

[server.control]
bind_addr = "0.0.0.0:2333"

# Every visitor connection is spread over 4 parallel data channels: the
# server gathers 4 pooled channels per visitor and labels each one as a
# stripe of one group. Both ends are this binary, so the striped
# data-channel framing is exercised end to end.
[server.data]
stripe_count = 4
