# Data-plane fixture: the client dials `[client.data].default_addr`, which differs
# from the control endpoint, so the server binds a dedicated data listener
# (`[server.data].bind_addr`) instead of reusing the control listener.

[client]
default_token = "default_token_if_not_specify" # security-scan:allow dummy fixture token, same value as the sibling transport fixtures

[client.control]
default_remote_addr = "127.0.0.1:2338"

[client.data]
default_data_addr = "127.0.0.1:2344"
default_mode = "multiplex"
default_count = 2

[client.transport]
type = "plain"

[client.services.echo]
local_addr = "127.0.0.1:8080"
remote_bind_addr = "0.0.0.0:2334"

[client.services.pingpong]
local_addr = "127.0.0.1:8081"
remote_bind_addr = "0.0.0.0:2335"

[server]
default_token = "default_token_if_not_specify" # security-scan:allow dummy fixture token, same value as the sibling transport fixtures
allow_ports = ["2334-2337"]

[server.control]
bind_addr = "0.0.0.0:2338"

[server.data]
bind_addr = "127.0.0.1:2344"

