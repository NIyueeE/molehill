# Ruff configuration for the python bench/test entries (uv/PEP 723 scripts
# under benches/scripts/). Linted by `uvx ruff check` and format-checked by
# `uvx ruff format --check` in the pre-commit gate (githooks/pre-commit) —
# see docs/checks.md.
target-version = "py311"
line-length = 88

[lint]
extend-select = [
    "E402",   # sys.path.insert before imports is the bench-lib pattern
    "I",      # import sorting
    "UP",     # pyupgrade
    "SIM",    # simplify
    "EXE",    # shebang + exec bit must agree
    "BLE",    # blind-except audit (each waiver is inline at its own site)
    "PL",     # pylint rules
    "RUF",    # ruff-specific
    "ISC",    # implicit string concat
    "FURB",   # refurb
    "S",      # bandit
]

# Waivers — repo-wide only for the one property that is genuinely true of
# every script here. Everything else is waived inline at its own site with a
# reason (AGENTS.md §2: fix the code first, waive in code, minimal scope).
#
# - S602/S603/S607: these scripts measure tools that ARE external binaries
#   reached through PATH (iperf3, tc, ss, vegeta, the peers). Resolving them
#   differently would measure a different program than the one documented.
ignore = ["S602", "S603", "S607"]
